# 规格实现对照矩阵

> **目的**：把"产品规格书"映射到"Web 原型"和"小程序"的实际实现位置，作为开发与评审时的对照清单。
>
> **由来**：MEM-52 流程反省发现，F3"事项管理"在小程序中走偏了 3 个 commit 才被识别。根因之一是缺少"规格 → 实现"对照清单。
>
> **维护规则**：
> 1. 新增/修改任何 P0/P1 功能前，先在此表确认"规格要求"和"原型实现位置"
> 2. 实现完成后，回填"小程序实现位置"列
> 3. 三列任何一处出现 ❌ 或 ⚠️，必须在 PR 描述中说明原因或开 issue 跟踪

---

## F1：今日确认面板（首页）

| 规格要求 (`product-spec.md` F1) | Web 原型 (`prototype.html`) | 小程序 (`miniprogram/`) | 状态 |
|---------------------------------|----------------------------|-------------------------|------|
| 首页展示所有事项今日状态 | ✅ `#page-today .confirm-panel` | ✅ `pages/index/index.wxml` | ✅ |
| 名称 + ✅/⏳ + 确认时间 | ✅ `.confirm-item` | ✅ `confirm-item` view | ✅ |
| 每日 00:00 自动重置 | ✅ JS 模拟 | ✅ `app.ts` 日期检测 | ✅ |
| 空状态：预设模板 + "添加事项"引导 | ✅ | ✅ `utils/constants.ts` | ✅ |

---

## F2：确认操作

| 规格要求 (`product-spec.md` F2) | Web 原型 | 小程序 | 状态 |
|---------------------------------|----------|--------|------|
| 按钮确认（1 次点击） | ✅ `.btn-secondary` 快速确认 | ✅ `onQuickConfirm` | ✅ |
| 拍照确认（1 次点击 + 拍照） | ✅ `.btn-primary` 拍照确认 | ✅ `onPhotoConfirm` (MEM-21) | ✅ |
| 记录：事项名 + 时间 + 方式 + 照片 | ✅ | ✅ | ✅ |
| 同日多次确认保留最近一次 | ✅ | ✅ `_saveConfirmation` 覆盖 | ✅ |
| 确认后即时显示 ✅ + 时间 | ✅ | ✅ | ✅ |

---

## F3：事项管理 ⚠️ **小程序偏离规格，MEM-49 修复中**

| 规格要求 (`product-spec.md` F3) | Web 原型 (`prototype.html`) | 小程序 (`miniprogram/`) | 状态 |
|---------------------------------|----------------------------|-------------------------|------|
| **添加入口位置**：今日页底部"添加新事项"行（US4 场景 4 明确） | ✅ line 1343 `.add-item` | ⚠️ 今日页 + 设置页两处入口（违反规格） | ❌ MEM-49 |
| **添加事项表单**：名称必填 + 图标可选 | ✅ P2 添加事项页（line 1546） | ⚠️ 今日页用 `wx.showModal` 简陋表单（无图标）；设置页用 `item-editor` 完整表单 | ❌ MEM-49 |
| **预设模板**：锁车、服药 | ✅ | ✅ `utils/constants.ts` | ✅ |
| **事项右侧 `+` 按钮**（加同类型确认槽，如服药 #1/#2） | ✅ line 1338 `addEntry()` | ❌ 缺失（`index.wxml:46-49` 只有 ✓/×） | ❌ MEM-49 |
| **设置页"管理事项"模块** | ❌ 不存在（符合规格） | ⚠️ 存在（`settings.wxml:3` — 规格未要求） | ❌ MEM-49 |
| **编辑事项** | ⚠️ 编辑模式仅有删除（MEM-50） | ✅ `item-editor` 组件 (MEM-33) | 🟡 |
| **删除事项** | ✅ 编辑模式 | ✅ 设置页 / 今日页编辑模式 | ✅ |
| **名称最多 10 字符** | ❌ 无校验 (MEM-48) | ✅ `slice(0, 10)` 截断 | 🟡 |
| **数量上限 10 个** | ❌ 无校验 (MEM-48) | ✅ `ITEM_MAX_COUNT` 检查 | 🟡 |
| **拖拽排序**（Post-MVP） | ❌ | ❌ | — |

---

## F4：历史记录

| 规格要求 (`product-spec.md` F4) | Web 原型 | 小程序 | 状态 |
|---------------------------------|----------|--------|------|
| 日期 + 各事项确认状态 + 时间 | ✅ `#page-history` | ✅ `pages/history` | ✅ |
| 按事项筛选 | ✅ | ✅ MEM-31 item picker | ✅ |
| 按日期范围筛选 | ❌ 仅原型未实现 | ✅ MEM-31 date-range picker | 🟡 小程序增强 |
| 本地存储 | ✅ localStorage | ✅ `wx.setStorageSync` | ✅ |

---

## F5：数据存储

| 规格要求 (`product-spec.md` F5) | Web 原型 | 小程序 | 状态 |
|---------------------------------|----------|--------|------|
| 本地优先策略 | ✅ localStorage | ✅ `utils/storage.ts` | ✅ |
| 事项 + 确认记录 + 照片本地 | ✅ | ✅ `wx.saveFile` 路径 | ✅ |
| 照片仅本地不上传 | ✅ | ✅ MVP 阶段未接云 | ✅ |

---

## 状态图例

- ✅ 三方一致，符合规格
- 🟡 双端存在差异但已知（如小程序增强、单端未实现）— 必须有 issue 跟踪
- ⚠️ 实现与规格存在偏差但未被识别为 bug — 必须重新评估
- ❌ 明确违反规格 — 必须开 issue 修复

---

## 已开 issue 跟踪

| Issue | 关联功能 | 状态 |
|-------|---------|------|
| MEM-46 | F3 添加事项 gap 修复总线 | Backlog |
| MEM-47 | F3 信息架构决策（方案 A） | Decided |
| MEM-48 | F3 P0 缺陷（字符/上限/XSS/空名） | Backlog |
| MEM-49 | F3 小程序回归规格 | Backlog |
| MEM-50 | F3 规格对齐（编辑/导航/令牌） | Backlog |
| MEM-51 | F3 视觉细节 | Backlog |
| MEM-52 | 流程反省 | Backlog |

---

## 更新历史

| 日期 | 变更 | 触发 |
|------|------|------|
| 2026-05-31 | 初版创建，F1-F5 全量盘点 | MEM-52 反省 |
